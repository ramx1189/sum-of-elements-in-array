#include <stdio.h>
int main(){
int n, sum=0;
printf("enter the number of elements:"}
scanf("%d", &n);
int arr[n];
printf("enter %d elements", n);
for(int i=0;i<n;i++){
scanf("%d", &arr[i]);
sum +=arr[i];
}
printf("sum of elements: %d\n",sum);
return 0;
}